# Styles

Global design tokens and utilities are defined in `app/globals.css` (required by Next.js App Router).

Import that file from `app/layout.tsx`. Feature-specific CSS should remain colocated with components or use Tailwind utility classes.
